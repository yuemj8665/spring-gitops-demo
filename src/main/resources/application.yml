server:
  port: 8080

spring:
  application:
    name: spring-gitops-demo

app:
  message: ${APP_MESSAGE:Hello from Spring Boot!}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
