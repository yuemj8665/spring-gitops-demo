apiVersion: v1
kind: Service
metadata:
  name: spring-gitops-demo-service
  labels:
    app: spring-gitops-demo
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: spring-gitops-demo
